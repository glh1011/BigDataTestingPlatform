import * as constants from './constants';

const defaultState = {
   // url: localStorage.getItem('url'),       
   // j_password: localStorage.getItem('j_password'),
   // j_username:localStorage.getItem('j_username'),
};
export default(state=defaultState) => {
      return state;
  }
